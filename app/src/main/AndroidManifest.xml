<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/tokopediaawal"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/tokopediaawal"
        android:supportsRtl="true"
        android:theme="@style/Theme.Testcarelogikadata"
        tools:targetApi="31">

        <!-- SplashActivity dijadikan Launcher -->
        <activity
            android:name=".SplashActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".FragmentFeed"
            android:exported="false" />
        <activity
            android:name=".FragmentBuatKamu"
            android:exported="false" />
        <activity
            android:name=".MainActivity"
            android:exported="true" />
    </application>

</manifest>